div(class='container' id='main')
div(class='navbar navbar-inverse navbar-fixed-top')
  div(class='container')
      h3(class='navbar-text') Welcome, Haleakala Admin
      ul(class='nav navbar-nav pull-right')
        div(class='btn btn-default dropdown')
          span(href='#' class='dropdown-toggle' data-toggle='dropdown')
            span(class='glyphicon glyphicon-user')
            | My Account
            strong(class='caret')
          ul(class='dropdown-menu')
            //Nigel when you click on this a, you should open a dialog
            a(class='btn' onclick='settingsDialog()')
              span(class='glyphicon glyphicon-wrench') Settings
            br
            form(action='/schedule/logout')
              button(type='submit' class='btn btn-default')
                span(class='glyphicon glyphicon-off') Logout